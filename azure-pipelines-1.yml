# Retired legacy entry point.
# The active service pipeline is /azure-pipelines.yml.
trigger: none
pr: none

stages:
- stage: Retired
  displayName: 'Retired pipeline entry point'
  jobs:
  - job: Retired
    displayName: 'Use /azure-pipelines.yml'
    steps:
    - checkout: none
